// @flow

// This file is only used in react-native projects
// React native doesn't have to be in (peer-)dependencies.
// $FlowFixMe
import { AsyncStorage } from 'react-native'

export { AsyncStorage as storage }
